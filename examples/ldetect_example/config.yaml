chromosome: "chr2"
region_start: 39967768
region_end: 40067768

n_individuals: 379    # EUR Phase 1 individuals in eurinds.txt
ne: 11418.0
cov_cutoff: 1.0e-7
n_snps_bw_bpoints: 50
subset: fourier_ls

bitbucket_base: "https://bitbucket.org/nygcresearch/ldetect/raw/master/ldetect/examples/example_data"

ref_dir: "ref"
work_dir: "work"
results_dir: "results"

# Optional runtime comparison from a VCF. The default reproduction uses the
# reference covariance file because the original example VCF is not archived.
# Set this to a local bgzipped+tabixed VCF to add a direct-vector vs
# covariance+matrix-to-vector benchmark target.
runtime_benchmark_reference_panel:
runtime_benchmark_vcf_chromosome: "2"
