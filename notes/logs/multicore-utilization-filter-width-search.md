# Multicore Utilization: Filter-Width Search Bottleneck

**Agent-oriented working log.** Raw, dated investigation notes — not proofread for external readability. For current, human-readable status, see `notes/findings/`.

Date: 2026-07-07

## Context

Following the cyvcf2 VCF I/O migration and the local-search OOM fix (grouped-partition process-pool dispatch, see `docs/optimizations.md` #5), asked whether there were further multicore-utilization improvements available in `ldetect run`.

## Timeline plot revealed an uninstrumented, single-threaded gap

`examples/ldetect_original/plots/EUR-chr21-timeline.pdf` (generated by `scripts/profile_run.py` + `scripts/plot_profile_timeline.py`) showed a large, unlabeled flat span between step 3 (matrix-to-vector) and step 4's "fourier metric" phase: RSS flat at ~260 MiB, CPU flat at ~100% (single core), lasting ~34 of ~64 total seconds for a chr21 EUR run — over half the total wall-clock.

`plot_profile_timeline.py`'s `_PHASES` list only rendered spans with matching `log_memory_checkpoint` start/end pairs, and nothing in `find_breakpoints` (`pipeline.py`) checkpointed the code between `step3_end` and `fourier_metric_start`: reading the vector, `custom_binary_search_with_trackback` (the Hanning filter-width search), and the final `apply_filter`/`get_minima_loc` call. Added three checkpoint pairs (`vector_read_start/end`, `filter_width_search_start/end`, `minima_extraction_start/end`) and matching phases in the plotting script.

Re-running the profiling job confirmed: **`filter_width_search` alone accounts for ~34s**, entirely single-threaded. The re-run log (`plots/EUR-chr21.log`) shows 41 separate `apply_filter` calls in that span (4 exponential search, ~13 binary search, ~24 coarse+fine trackback), each ~1s at widths in the 8,000-18,000 range.

## Root cause: `scipy.ndimage.convolve1d` is a direct (non-FFT) convolution

Cost is O(N·width). Since the exponential search doubles the candidate width and the trackback sweep refines near the converged width (which was ~9,000 for this chromosome), most of the 41 calls fall in the expensive range.

## FFT convolution: tried, reverted — unsafe

Replacing `scipy.ndimage.convolve1d` with `scipy.signal.fftconvolve` (`np.pad(..., mode="symmetric")` to match `convolve1d`'s default `reflect` boundary, verified to agree with direct convolution to ~1e-14 absolute error on random noisy data, and ~100-150x faster at realistic widths) looked like a clean win.

**But it broke 6 of `tests/test_filters.py`'s existing tests**, going from 1 detected minimum to 23 on a synthetic flat-plateau-plus-valley fixture. Root cause: `scipy.ndimage.convolve1d`'s direct convolution produces bit-identical output at every position across a constant/flat input stretch (same shift-invariant arithmetic repeated) — so `argrelextrema`'s strict `<` comparison never fires there. FFT convolution is a *global* transform; its rounding error is not shift-invariant, so it injects distinct ~1e-15 noise at every position, breaking exact ties and manufacturing spurious local minima out of nothing.

Confirmed directly:
```python
arr = np.ones(200) * 5.0; arr[40:61] = <valley>
direct = ndimage.convolve1d(arr, window)  # exactly 5.0 at every flat-region position
# FFT version: same region prints "5. 5. 5. ..." but differs from 5.0 by ~1e-15 per position
```

Since the binary/trackback search is actively hunting for a width producing exactly N minima, spurious minima anywhere would derail convergence to a different `found_width` and a different breakpoint set entirely — real covariance-sum vectors can plausibly have flat/tied runs (sparse regions, zero-padding), so this isn't just a synthetic-fixture artifact. **Reverted in full** (`filters.py` and `tests/test_filters.py` back to the pre-FFT state, confirmed via `git diff --stat` showing no changes).

Other FFT-family options (`scipy.signal.oaconvolve`) share the same global-transform rounding-noise characteristic and would have the same flaw. A numba `prange`-parallel *direct* convolution (same summation structure, hence flat-region-safe by construction) was considered as a bigger-lift alternative but not pursued this session — it would need exact boundary-condition replication and real-data validation to trust as bit-identical to the current output on non-flat data.

## Shipped: thread-parallelize the search's independent-candidate rounds

Confirmed empirically that `scipy.ndimage.convolve1d`/`argrelextrema` release the GIL enough for real `ThreadPoolExecutor` speedup (~5x with 8 threads on 8 independent calls, results identical to sequential).

Two of the three search phases evaluate a boundable/predictable set of candidates per round and can be batched without changing the decision logic at all — same numerics, same final result, just computed concurrently instead of one-at-a-time:

- **`_find_end`** (exponential search, doubling): batches up to `max_workers` doubling candidates per round, returns the smallest satisfying `f(x) < val` — identical to the sequential recursive doubling.
- **`_trackback`** (coarse + fine refinement sweep): batches each round's candidate window in chunks of `max_workers`, applies the original first-match-wins decision rule in the same left-to-right order over the concurrently-computed results.

**Not parallelized**: the core binary search (`find_le_ind`, `_util/binary_search.py`). Each of its steps is adaptive on the previous comparison (can't be pre-batched without a fundamentally different k-ary-search algorithm), and it's a shared utility used elsewhere — changing its core algorithm was judged out of scope for this pass given the added verification burden versus the smaller share of total calls it represents (~13 of 41 in the observed trace).

Wired via a new `search_workers` parameter on `custom_binary_search_with_trackback`, reusing `find_breakpoints`'s existing `workers` argument (already meant for local search, which hasn't started yet at the point the filter-width search runs — no oversubscription risk, confirmed via the flat single-core CPU/RSS in the profiling plot showing no other multiprocessing active during this phase). Later MacDonald2022 diagnostics exposed that batching the exponential doubling phase was a mistake with large `workers`: it could evaluate very large, very expensive widths after an earlier candidate in the same batch already satisfied the stopping rule. `_find_end` is now sequential; trackback remains threaded.

Verified via `tests/test_find_minima.py`: `test_trackback_threaded_matches_sequential`, `test_find_end_ignores_workers_and_matches_sequential`, `test_find_end_stops_before_later_doubling_candidates`, `test_find_end_threaded_raises_at_max_srch_val` — threaded (`search_workers=4`) vs. sequential (`search_workers=1`) produce identical `found_width` and identical error behavior, while `_find_end` stops before later doubling candidates. Full suite (227 tests), ruff, mypy all clean.

## Still open (as of the thread-parallelization pass above)

- Real-data validation of the actual wall-clock improvement (this was validated for correctness locally; the ~34s chr21 phase's real speedup under `--workers N` hasn't been re-profiled on the cluster yet).
- The binary-search phase itself (~13/41 calls) remains single-threaded.
- A numba direct convolution (flat-region-safe, unlike FFT) as a bigger structural fix, not attempted this session.

## Follow-up: real-cluster re-profiling confirmed the threading fix, pinpointed the remaining cost

Asked the user to pull the thread-parallelization commit and re-profile chr21 on the cluster. First attempt still showed no improvement — root-caused (by the user) to a copy-pasted `--output-dir` typo reusing a stale chr2 output directory, not a code issue; the README's own documented command was already correct. A clean re-run's log/plot showed the fix *did* work, exactly as scoped: `filter_width_search` dropped from ~34s to ~20s. Log timestamps showed the exponential+binary-search portion (17 calls, unparallelized by design) taking a fixed ~13s (~1 call/sec), while the trackback portion (24 calls) showed multiple calls landing in the same one-second timestamp repeatedly — direct evidence of real concurrency — completing in ~7s (~3.4x speedup, consistent with `search_workers=4`). This confirmed the ~13s exponential+binary-search portion, un-touchable by thread-parallelization (adaptive, one-call-at-a-time), was now the dominant remaining cost, and motivated the numba pass below.

## Numba direct convolution: implementation and a real snag caught by tests

Planned via Explore + Plan subagents (numba conventions in `shrinkage.py`, scipy `mode='reflect'` boundary semantics, blast radius, GIL-release constraint). Key pre-implementation findings from the Plan agent's own scratch verification: `numpy.pad` is not numba-jittable (must hand-roll the reflect-boundary index formula); the reflect formula was verified bit-exact against `np.pad(mode="symmetric")` including `width >= len(array)` cycling; a GIL-release check via a background counter-thread (not wall-clock speedup, which is CI-flaky) was validated as a reliable technique (~2.7M increments with `nogil=True` vs. ~127K without, during a fixed workload).

Implemented `_reflect_index`, `_pad_reflect`, `_convolve1d_reflect` in `filters.py`, `@njit(nogil=True, cache=True)`, scope limited to this (no `prange`/`parallel=True` — see `notes/findings/multicore-utilization.md` for why that was deferred).

**Caught by the test suite, not assumed away**: `test_apply_filter_larger_width_fewer_minima` failed (20 vs 28 minima, backwards from expected) even though the smoothed array matched scipy to ~2.7e-15 (no logic bug) and the flat-plateau fixtures (`_ARR`/`_ARR2`) still passed exactly. Root-caused: the test's width=2-vs-50 comparison was already an exact tie under *plain scipy* (20==20) — sweeping widths 2-95 under scipy alone showed minima count is genuinely non-monotonic at intermediate widths for this periodic synthetic signal (20, 18, 15, 21, 25, 24, 20, 18, 9, 3). This is a distinct, more general instability than the FFT bug: near-tied *output* extrema (from smoothing structure, not from constant input) are sensitive to any implementation-level floating-point difference, numba or not. De-risked for realistic data: 0 mismatches across 80 randomized noisy-random-walk trials at production-representative widths (up to 9,000). Fixed the test by widening to a decisive width pair (2 vs. 95: 20 vs. 3 under both implementations) rather than weakening the assertion.

**Speed snag, also caught before declaring victory**: initial `nogil=True, cache=True` (no `fastmath`) measured ~2x *slower* than `scipy.ndimage.convolve1d` at realistic widths — LLVM wasn't auto-vectorizing the strict-IEEE-order reduction loop. Adding `fastmath=True` flipped this to ~2x *faster*, and was re-verified not to reopen flat-region risk (fastmath reassociation is fixed at compile time and applied identically at every position — still shift-invariant, still produces bit-identical output on a constant input run, confirmed empirically).

**Final measured result**: ~2x per-call speedup over scipy at realistic widths (8,000-18,000), multiplicative with the already-shipped ~5x threading — combined ~10x over the original sequential-scipy baseline on an 8-candidate batch. Added padding-equivalence, convolution-value-equivalence, exact minima-index-equivalence (scipy vs. numba, not just closeness), degenerate-width, and GIL-release regression tests to `tests/test_filters.py` (12 → 33 tests in that file). Full suite 248 tests, ruff, mypy all clean.

## Follow-up: fixed a bad numba-unavailable fallback before it could bite

User caught that `_njit_nogil`'s `ImportError` fallback (mirroring `shrinkage.py`'s existing pattern) would silently leave `_convolve1d_reflect` as an *un-jitted plain-Python* O(N·width) triple-nested loop if numba were ever missing — ~10^8 iterations at production widths, catastrophically slow, not just non-optimal. Numba is a hard dependency (`pyproject.toml`), so this should be unreachable in practice, but "should be unreachable" isn't the same as "safe if it happens." Fixed: `apply_filter` now checks `_HAVE_NUMBA` and calls the original `scipy.ndimage.convolve1d` directly in that branch, so a broken/tampered environment degrades to "non-optimal" (same as before this whole optimization pass) rather than "unusable." Added `test_apply_filter_falls_back_to_scipy_when_numba_unavailable` (monkeypatches `_HAVE_NUMBA`, since uninstalling numba isn't practical in CI). 249 tests, ruff, mypy clean.

Also investigated, prompted by a direct question, whether `fastmath=True` introduces meaningfully more inexactness than the numba migration already does: measured no-fastmath vs. fastmath maxdiff against scipy directly (both ~1e-13 to ~1e-12 absolute on a signal of scale ~100, fastmath if anything slightly *smaller* here due to FMA's precision gain partly offsetting reassociation) — `fastmath` doesn't add a new risk category, just marginal noise at the same order of magnitude the numba-vs-scipy switch already introduces.

Also investigated whether the `prange`-oversubscription concern (cited above as the reason to defer within-call multicore) is empirically real: composes fine with `nogil`/`fastmath` (no crashes), and a quick local test (unconstrained + a numba-thread-count-limited simulation) showed *no* measurable oversubscription penalty when combined with the outer `ThreadPoolExecutor` — if anything slightly faster. Could not fully replicate a genuinely CPU-affinity-constrained cluster allocation locally (no cgroups/taskset control available), so this isn't a full exoneration, but the real blocker for deferring `prange` is the API-surface refactor cost (per-phase-different filter functions), not a demonstrated performance hazard — worth revisiting if more speedup is wanted later.

## Follow-up idea after MacDonald2022 pipeline fixes: split filter parallelism by search phase

Date: 2026-07-26

During the MacDonald2022 deCODE replication/debugging pass, we revisited whether a simple `--filter-workers N` knob should replace candidate-width threading during filter-width search. The better follow-up design is probably not either/or across the entire search, but a split policy by phase:

- **Exponential search**: width choices are adaptive and must be evaluated one at a time, so within-convolution `numba.prange` is the only useful parallel layer.
- **Binary search**: also adaptive and one width at a time, so it can likewise benefit from `prange` without any outer candidate-width pool.
- **Trackback**: has a small but explicit candidate set per scan window (default coarse scan: 9 candidate widths; default fine scan: 19 candidate widths). Candidate-width threading is a natural coarse-grained parallel layer here and is likely preferable to `prange` for that phase.

This suggests a future implementation shape:

```text
--workers N --filter-workers M

exponential search: one width at a time, each convolution uses M numba threads
binary search:      one width at a time, each convolution uses M numba threads
trackback:          N candidate widths at a time, each convolution uses 1 numba thread
```

That would avoid nested parallelism while preserving the trackback speedup we already know helps. It also lets the adaptive phases use more than one core, which candidate-width threading cannot do there by construction.

Do not pick this up until after the MacDonald2022 pipeline is stable: it touches shared `find_minima.py` plumbing (`custom_binary_search_with_trackback`, `FlexibleBoundedAccessor`, and the `f: Callable` interface) and should be benchmarked against all three modes:

```text
--workers N --filter-workers 1      # current candidate-width-only baseline
--workers 1 --filter-workers M      # pure prange baseline
--workers N --filter-workers M      # split policy
```

Key correctness checks when this is implemented: exact `found_width`, exact minima loci, exact BED output on the chr21 deCODE replication diagnostic, and no increase in active thread count during trackback beyond the intended outer candidate pool.

## Follow-up: deprecate and eventually remove symmetric Hann mode

Date: 2026-07-26

The MacDonald2022 chr21 deCODE exact-match diagnostic confirmed that original ldetect's Step 4 behavior uses SciPy's periodic Hann window (`scipy.signal.get_window("hann", n)`, equivalent to `fftbins=True`), not NumPy's symmetric `np.hanning(n)` window. ldetect-lite now defaults to `scipy-periodic` across the CLI, `find_breakpoints()`, and filter helper APIs.

Keep `--filter-window symmetric` only as a deprecated compatibility/diagnostic knob for historical output comparisons. It should not be treated as a peer "correct" mode long term. Once old-output comparisons no longer need it, plan a removal pass:

- remove `symmetric` from CLI choices and public filter helper modes;
- simplify tests that only exist to preserve symmetric-vs-periodic branching;
- update docs to describe the single Hann behavior as original-ldetect-compatible;
- keep historical notes explaining why older outputs using `np.hanning` may differ.

**Correction (2026-07-28): do not act on the removal plan above.** This entry's
"original ldetect uses periodic Hann" conclusion was based only on the
MacDonald2022 chr21 deCODE diagnostic and over-generalized to "original
ldetect" broadly. A later investigation (see
`notes/logs/ldetect-original-main-pipeline-audit.md`, "Regression reopened"
section, and `notes/findings/ldetect-original-reproduction.md`) found that
Berisa & Pickrell's *actual* 2015 analysis computed the *symmetric* window
despite their code requesting periodic, because scipy 0.16.0 (their
contemporary release) had a confirmed defect making periodic and symmetric
windows bit-identical for odd-length windows — fixed in scipy 1.1.0 (2018).
`symmetric` is required, not deprecated, for `ldetect_original`; `scipy-periodic`
is correct for MacDonald2022. Both are permanent, equally-valid modes for
their respective reproduction targets. Do not remove `symmetric`.

## Follow-up: `prange` split shipped, measured as ineffective, root-caused, and replaced

Date: 2026-07-26

Implemented the split-phase design from the previous entry: `prange` over output rows for the adaptive exponential/binary-search/minima-extraction phases, unchanged candidate-width threading (forced to 1 numba thread per candidate) for trackback. Shipped across two commits (`Added prange split`, `Fixed oversubscription issue the last patch created`).

Asked the user to profile it on the real cluster with an isolated A/B (`--workers 1 --filter-workers 1` vs. `--workers 1 --filter-workers 4`, isolating this from every other pipeline stage) as a follow-up to a prior comparison that showed no change at `--workers 4` production settings. Result: **no measurable wall-clock difference either way** — the binary-search sub-phase took the same time before and after — despite CPU utilization during that window jumping from a flat ~100% to ~380-400%, confirmed via the timeline plot and `profile_run.py` CSV trace. Real thread-level parallelism was happening; it just wasn't turning into throughput.

Investigated with a standalone synthetic kernel microbenchmark (`examples/ldetect_original/scripts/benchmark_filter_threading.py`, no reference panel/VCF needed — just `_convolve1d_reflect`/`_convolve1d_reflect_parallel` called directly with synthetic arrays at real chr21 scale). Ruled out two hypotheses in order before finding the real one:

1. **Per-call `set_num_threads` reset overhead** (the oversubscription fix resets Numba's active thread count to 1 after every call). Measured "warm" (thread count set once, left running) vs. "reset-per-call" parallel timing: identical. Not the cause.
2. **Threading-layer choice** (`workqueue`, the only layer installable on macOS arm64, vs. `tbb`, prebuilt-wheel-installable on the user's x86_64 cluster). The user's host already had `tbb` active by default; re-testing with it explicitly reinstalled showed the same ~1.2x-at-4-threads ceiling. Not the cause either — and this ruled out threading-layer choice as a fix path entirely.

A thread-count sweep (1/2/4/8 threads, capped at 4 by the cluster session's Numba-reified thread count that run) showed the `prange` kernel's timings fit `fixed_overhead + compute/threads` almost exactly with `fixed_overhead ≈ 0` and `compute ≈ 1897ms` — but the true serial (non-`parallel=True`) kernel does the same work in `563ms`. So `prange`'s per-thread compute throughput was **~3.4x worse** than the plain serial kernel doing identical work, and `4 threads / 3.37x-worse-per-thread ≈ 1.19x` reproduced the measured ceiling almost exactly. Getting more cores would not have helped — it would take ~13-14 of these degraded threads to match a true 4x speedup over serial.

Narrowed the vectorization gap further with a minimal isolated benchmark comparing three variants of the identical loop body: (a) output array allocated locally inside the `njit` function (matches `_convolve1d_reflect`) — fast; (b) output array passed in as a parameter, loop still `range(n)` — equally fast (ruled out "output as a parameter" as the cause); (c) output array passed in, loop as `range(lo, hi)` with `lo`/`hi` as runtime parameters — **~3.4x slower**, reproducing the regression exactly with no threading involved at all. Root cause: Numba's LLVM auto-vectorization of this reduction loop only fires for a literal `range(n)` loop starting at a compile-time-known zero; a runtime lower bound defeats it. `prange`'s internally generated per-thread chunk loop has exactly this shape, which is why it hit the same wall regardless of thread count or threading layer. Confirmed the fix in the same isolated benchmark: slicing the padded input and output arrays to zero-based views *before* the call (repositioning the pointer instead of the loop bound) fully restored vectorized single-thread throughput, including for a genuinely offset slice.

**Fix:** removed `_convolve1d_reflect_parallel` (`@njit(parallel=True)`/`prange`) and `_numba_thread_limit`/`set_num_threads` entirely. Added `_convolve1d_reflect_range` (plain `nogil`+`fastmath`, operating on pre-sliced zero-based views) and `_convolve1d_reflect_threaded` (splits output rows into `workers` contiguous chunks via `_chunk_bounds`, slices the arrays per chunk, dispatches via a cached grow-only `ThreadPoolExecutor`). Renamed the `*_numba_parallel` wrapper functions to `*_threaded` since "Numba-parallel" no longer describes the mechanism. Reduction order per row is unchanged (each row is still computed independently), so this carries no new numerical risk beyond what `#11`/`#12` already validated — covered by the existing exactness suite plus new parametrized tests (`test_threaded_convolve1d_reflect_matches_serial_for_asymmetric_kernel` at 1/2/3/4/8 workers, `test_chunk_bounds_partitions_range_exactly`). Full suite (320 tests), ruff, mypy all clean.

**Measured impact**, N=230,000 (real chr21 vector scale), width=9,000 (real production width), same cluster host: `prange` topped out at 1.19x at 4 threads (1/2/4/8 threads: 0.30x/0.59x/1.19x, capped at 4 by the session's Numba thread limit); the row-chunked replacement measured 1.00x/1.98x/3.92x/7.64x at 1/2/4/8 threads — real, close-to-linear scaling, and no longer capped at 4 threads since there's no more Numba-internal thread pool to reify a ceiling from. On the real chr21 pipeline at `--workers 4`: the exponential+binary-search portion of `filter_width_search` dropped from 5s to 1s (~5x); the whole phase (including still-serial-by-design trackback) from 7s to 4s (~43%).

See `docs/optimizations.md` #15 and `notes/findings/multicore-utilization.md` for the distilled summary.

## Still open (current)

- Real-cluster wall-clock re-validation of the numba kernel specifically (thread-parallelization was already re-validated on the cluster above; the numba speedup is only measured locally so far).
- A fresh chr21 profiling run + breakpoints/BED byte-exactness diff against the pre-numba saved outputs (`plots/EUR_LD_blocks.bed`, `EUR_raw_LD_blocks.bed`) before considering this fully production-validated.
- The binary-search phase itself remains unparallelized (adaptive, shared utility) — now benefits from the numba per-call speedup even though it isn't itself parallelized.
- The row-chunked threaded convolution that replaced `prange` (see above) is validated on an isolated chr21 run and a standalone kernel microbenchmark, but not yet on the full 22-chromosome x 3-population replication.
- Deprecate and eventually remove the temporary `symmetric` Hann-window mode after MacDonald2022 and ldetect_original reruns are stable under the original-ldetect-compatible periodic default.
